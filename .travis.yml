language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t sultan/react-test -f ./client/Dockerfile.dev ./client
script:
  - docker run -e CI=true sultan/react-test npm run test
